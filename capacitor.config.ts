import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shehabsaad.readly',
  appName: 'npm run buildnpx cap add androidnpx cap sync androidcd androidgradlew assembleDebug',
  webDir: 'dist'
};

export default config;
